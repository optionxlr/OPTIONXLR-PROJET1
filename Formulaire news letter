export default function FormulaireNewsletter() {
  return (
    <form className="flex flex-col sm:flex-row justify-center gap-4">
      <input
        type="email"
        placeholder="Votre email"
        className="px-4 py-2 rounded bg-gray-700 text-white focus:outline-neon"
      />
      <button
        type="submit"
        className="px-6 py-2 rounded-full border border-neon text-neon hover:shadow-neon transition"
      >
        S’abonner
      </button>
    </form>
  )
}
